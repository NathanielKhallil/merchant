import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  button: {
    backgroundColor: "black",
    color: "white",
    fontWeight: "bold",
    "&:hover": {},
  },
}));
